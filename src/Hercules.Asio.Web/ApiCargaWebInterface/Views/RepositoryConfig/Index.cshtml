@model IEnumerable<ApiCargaWebInterface.ViewModels.RepositoryConfigViewModel>

@{
    ViewData["Title"] = "Repositorios";
    ViewData["BodyClass"] = "listado";
}

<div class="row">
    <div class="col col-12 col-lg-12 col-contenido">
        <div class="wrapCol">
            <div class="acciones-listado">
                <div class="wrap">

                </div>
            </div>
            <div class="resource-list compacView">
                <div class="wrap">
                    <h1>Administración > Repositorios</h1>
                    <p class="descripcionPagina">
                        En esta página se pueden gestionar los repositorios de datos de Gestión de Investigación conectados con ASIO. Puede crear un nuevo repositorio haciendo clic en el icono '+' y editar un repositorio haciendo clic en su nombre.
                    </p>
                    @foreach (var item in Model)
                    {
                        <article class="resource resource-ficharepositorio">
                            <div class="wrap">
                                <h2>
                                    <a href="@Url.Action("Details", new {  id=item.RepositoryConfigID  })">@Html.DisplayFor(modelItem => item.Name)</a>
                                </h2>
                                <div class="desc">
                                    <p class="tipo">@Html.DisplayFor(modelItem => item.Url)</p>
                                    @*<p>@Html.DisplayFor(modelItem => item.OauthToken)</p>*@
                                </div>
                            </div>
                        </article>
                    }
                </div>
            </div>
            <a href="@Url.Action("Create")" class="btn-mas-recursos">
                <em class="material-icons add">add</em>
            </a>
        </div>
    </div>
</div>
